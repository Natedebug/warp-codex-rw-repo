# data/

- `input.txt`: Example input that `scripts/read.sh` reads by default.
- Outputs created by `scripts/write.sh` are typically named `output.txt` (or any path you pass in) and are ignored by git per `.gitignore`.
