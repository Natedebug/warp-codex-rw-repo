# Personal Assistant permissions configuration (copy to permissions.env to use)
# Read access is enabled by default; set to 0 to disable file reads via pa.sh.
PA_ALLOW_READ=1

# Write access is scoped to safe locations. Set to 1 to allow writes; 0 blocks writes.
PA_ALLOW_WRITE=1

# Colon-separated list of directories where pa.sh is allowed to write.
# Expand ~ to your home directory as needed.
PA_ALLOWED_WRITE_DIRS="$HOME/Library/Application Support/pa-assistant:$HOME/Documents/personal-assistant:data/personal-assistant"

# Require confirmation before writing when running interactively (TTY). Default is disabled unless set here.
PA_CONFIRM_WRITE=1
